{
  "scripts": {
    "build": "mkdir -p public && cp index.html output.css public/ && cp -f README.md public/README.md || true"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
